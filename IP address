Introduction to IP Address:

1. There has to be way to identify where the request should go. IP address is essentially identity layer of networking.
Without an unique identifier packets would have no destination.

2. Some are designed for communication inside private network while others exposed services to the public internet. This distinction
becomes crucial.

3.Internet has evolved from IPV4 to IPV6. As billions of devices came online, the original address space become inefficient, forcing
the industry to rethink how global addressing works. 

4. When you design distributed system, IP address stop being a networking detail and become foundational building block.Every request,
service call and every connection ultimately depends on systems being able to find each other.


IPV4
-> IPV4 (Internet protocol version 4) is the most widely used addressing system. 32 bit address format. total address
available = 4.3 billion.  Uses : Tradiotional networking, web servers, and most current internet devices. challenges:
limited IPs, fragmentation and security concerns.

-> when users access your application, networking infrastructure uses IP addresses to determine where traffic should go 
and how should resposne return.

->internet eventually outgrew the assumptions on which IPv4 was built.

Ipv6

-> Ipv6 (Internet protocol version 6) next generation IP standard.

->128 - bit address format.Total address available = 340 undecillion (virtually unlimited)

-> Uses : IoT, mobile networks and future scalability.key benefits : large address space, better security,
improved routing efficiency. ipv6 is long term scalability.

Feature                      Ipv4                               Ipv6
Address size                  32 bit                            128 bit
Address format                decimal                           hexadecimal
Address space                 4.3 billion address               unlimited
Secuity                      relies on additional protocols     built in security
performance                  limited due to NAT & fragmentation   more efficient routing and handling
Adoption                     still widely used                   slowly being adopted

 -> Most production systems are deliberately designed so that the majority of the infrastructure remains hidden behind
the private networks. 

Public Ips
-> Assigned by Internet service provider (ISPs). Used to communicate over the internet. unique worldwide.

private Ips
 -> Used within local networks. cannot be accessed directly from the internet.
databases, caches, internal API's and backend services can communicate freely using private addresses. The public entry points
are load balancers, API gateways and reverse proxies.

-> A user reaches the public endpoint, but the request often travel through multiple layers of private infrastructure.
before reaching the service that actually processes the request.
-> public Ips provide reachability while private Ips provide isolation. Modern architecture rely on both to access scalaibility,
accessibility and security.

Why do we need private Ips?
-> Conserves public Ip address.
-> Enhances security (private Ips are not routable over the internet).
-> Enables Network Address Translation (NAT) to allow multiple devices to share a single public Ip.
-> Common in corporate networks, data centers and cloud environments.

Most systems should never be directly reachable from the internet. Databases, internal APIs, caching layers, backend services 
and message brokers are typically placed on private network where they can communicate internally while remain being hidden from
the external traffic.This design naturally leads to NAT where many private devices share a much smaller set of public address.
From the internet perspective traffic appears to be come from a public endpoint, while internally thousands of system may be 
operating behind it. cloud enviroments, data centers, corporate networks and kubernetes cluster all rely heavily on private addressing.
In most production systems, only a small number of components are publicly exposed, while the vast majority of the infrastructure
operates entirely on private networks.


Private Ips are not just addressing convinience. They are foundational mechanism for achieving scale, reducing tha attack surface
and building secured distributed system.

The role of Ip in system design is now at small scale, an Ip addresses how one machine finds other. At system design sclae, Ip 
addressing becomes part of the architecture itself.

Consider a gloabal application serving across multiple regions, request need to be routed to the right data center
and traffic must be distributed across many servers and services must communicate reliably across networks. Ip based routing and
load balancing mechanism make all that possible. security architecture also depends heavily on Ip boundaries. One of the
first decisions architetcs make is which system is publically reachable and which should remain private. Firewalls, VPNS,
network segmentation, zero trust design all rely on controlling communication path between IP ranges and in cloud environments
IP management becomes even more important. Why? because infrastrutcure is dynamic. Servers, containers and services are constantly
being created, scaled and replaced. 

The networking layer must adapt without disrupting communication between the components. thsi becomes especially visible in
microservices architecture. A single user request may travers dozens of services, most of which communicate exclusively over
private networks. users never see those internal Ips. But the entire application depends on functioning them correctly.

Avery common mistake is treating networking as a low level implementation detail. experienced architects view it differently.
Scalability, availability, security,disaster recovery, multi region deployment and service to service communication all depends
on networking decision. Ip addressing sits at the foundation of those decisions. the key take away is that Ips dont just
idetify the system. they influence how system scale, communicate, isolate the risk and opearte in production.

common system design interview questions on Ip:
1. How do IPv4 and IPV6 address differ?
2. Why do we need private Ips in system deisgn?
3. How NAT helps in addressing the ipv4 Shortage.
4.Explain load balancer distributes traffic using IPs.
5. How does DNS resolve IP addresses in a large scale system.






















